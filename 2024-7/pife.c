#include "stdio.h"
#include "stdlib.h"
int main()
{
    int n;
    scanf("%d",&n);
    char *str;
    str=(char *)malloc(1000*sizeof(char));
    scanf("%s",str);
    for(int i=0;i<n;i++)
    {
        
    }
}